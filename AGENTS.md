# AGENTS.md

## 1. 项目级约束

以下约束适用于整个仓库；具体设计、实现和审查规则由任务语义命中的项目技能提供，不在本文件重复维护。

- 仓库中的 Python 脚本统一使用 `python3` 运行；系统自带的 `python` 是 Python 2。
- 使用仓库固定的 Rust 2024 夜间工具链和 `rustfmt` 配置作为格式化事实来源。
- 可复用的内核、组件、内存、虚拟化和可移植驱动软件包优先使用 `#![no_std]`；只有软件包边界确实需要时才增加 `alloc`、`std` 或受功能开关控制的支持。
- 软件包和模块边界应与 TGOSKits 分层一致：可复用逻辑放在 `components/`、`drivers/`、`memory/` 或 `virtualization/`，操作系统适配代码放在使用它的 ArceOS、StarryOS、Axvisor 或平台层附近。
- 公共接口和共享代码中的注释使用英文；面向项目的文档、拉取请求、议题、审查回复和讨论使用中文说明技术逻辑，命令、路径、代码标识符和标准正式名称可以保留原文。
- 判断项目技能是否适用时以行为语义为准，不以文件路径、拉取请求标题或作者声明代替判断。上下文被压缩或无法确信仍完整记得适用技能时，继续前重新读取相应技能。

## 2. 项目工作流

以下规则约束验证、持续集成、拉取请求和项目协作流程。

### 2.1 本地验证

项目任务工具负责展开软件包选择、功能组合和目标矩阵，并统一汇总失败结果；本地验证应使用这些稳定入口。

- 按实际改动选择能覆盖受影响行为、功能组合和目标的验证，完成本节及适用技能要求的检查。检查通过后，仅在新增改动、失败或未解决风险需要时扩大或重复验证；纯文档或技能说明修改只检查差异、引用及适用的文档或技能校验，不运行 Rust 构建和测试。
- 修改 Rust 代码后使用项目适配的 `cargo xtask clippy` 执行静态检查。定向检查使用 `cargo xtask clippy --package <软件包>`，全工作区检查使用 `cargo xtask clippy` 或 `cargo xtask clippy --all`，增量检查使用 `cargo xtask clippy --since <引用>`；不得用原生 `cargo clippy` 命令代替。
- 修改 `scripts/test/std_crates.csv` 白名单覆盖的软件包后，使用 `cargo xtask test` 执行标准库测试；增量验证使用 `cargo xtask test --since <引用>`。该入口不支持按软件包选择，不得用原生 `cargo test` 命令代替。
- 不得通过增加 `allow` 属性来回避静态检查警告；除非用户明确要求，否则应修复根因。
- 编辑 Rust 代码后运行 `cargo fmt`。
- 修复错误时，先增加一个在错误实现上必然失败的确定性回归测试并验证失败，再实现或恢复修复，最后验证同一测试通过。不得只依赖修复后的证明、概率性复现程序或放宽后的测试。
- 构建、测试或运行 ArceOS、StarryOS 和 Axvisor 时，使用 `cargo xtask` 命令族，不直接使用 `cargo build`、`cargo test` 或 `cargo run`。
- 只有项目任务工具没有相应入口且任务明确需要特殊配置时，才检查 `xtask` 实现并使用原生 Cargo 命令手工匹配参数；不得把任务工具内部调用的原生命令当作项目验证入口。
- 解决变基或合并冲突时，不得手工合并发生冲突的 `Cargo.lock` 内容。先解决其他冲突，再用 Cargo 重新生成 `Cargo.lock` 并验证生成的锁文件。

### 2.2 持续集成与审查

持续集成配置和拉取请求审查使用仓库定义的证据链，不以临时本地命令或宽松检查替代项目门禁。

- `.github/workflows/ci.yml` 中自托管持续集成矩阵项的 `cache_key` 必须保持为空字符串（`cache_key: ""`）。非空值会在自托管运行器上启用 `rust-cache` 步骤，可能删除 Rust 或 Cargo 状态并破坏后续任务。
- 单项拉取请求审查执行 `review-single-pr`，批量审查执行 `review-open-prs`。在作出审查结论前，必须完整执行相应技能规定的持续集成前置门禁、技能路由、证据清单和当前提交复核。
- 执行拉取请求审查时不得运行本地格式化、构建、静态检查、测试、QEMU 测试、元数据、打包或发布验证命令。唯一运行时例外是技能明确允许的直接变更 `apps/**` 可运行应用，而且相同应用与目标缺少等价持续集成运行。
- 审查流程中的 `CI_DEFERRED` 和 `CI_SKIPPED` 是前置门禁状态，不是审查结论，不得发布到拉取请求。

### 2.3 拉取请求

面向项目的提交应保持中性、可追溯，并让标题、正文、验证记录和分支实际内容一致。

- 拉取请求标题遵循约定式提交的 `type(scope): content` 格式。一个软件包明显占主导时，优先把该软件包名作为 `scope`；跨领域或基础设施改动可以使用 `ci`、`repo` 或 `docs` 等较宽范围。
- 提交拉取请求时，标题使用英文，正文使用中文。正文必须说明要解决的问题、实际改动以及每一步方案背后的逻辑。
- 提交拉取请求前尽可能在本地验证持续集成流程；除非用户明确要求，只有实体板卡测试和自托管测试流程可以排除。只修改文档且不影响构建或测试时，不要求本地持续集成验证。
- 分支新增或修改提交后，更新拉取请求描述，使其与已提交改动保持同步。
- 除非用户明确要求，不得加入与代理相关的标签、签名、品牌或宣传性措辞，例如 `codex`、`agent`、`AI`。
- 修改体系结构启动逻辑、someboot 启动顺序、统一可扩展固件接口交接、对称多处理启动、动态平台契约、目标描述文件假设或推荐调试流程时，在同一改动中更新 `arch-platform-porting` 技能或其参考资料。

### 2.4 授权与完成条件

从当前请求和会话中已有约定确定任务范围与交付物。用户明确指令优先于项目技能指引，但不绕过上层规则、工具权限或平台限制；读取技能本身不增加任务权限。

- “帮我修改”“修复”等行动请求授权连续完成范围内的读取、实现、必要验证和结果说明；“修复并提 PR”还包括提交、推送和创建拉取请求。阶段切换不重复询问“是否继续”；只读分析请求在给出结论和证据后结束。
- 优先从上下文和可读取资料补足信息，普通实现选择自行判断并说明必要假设。只有无法推断且会改变正确性、范围或交付目标的信息，或尚未获授权的破坏性、不可逆及外部写入操作，才需要询问。先完成不依赖该答案的已授权准备工作，再针对具体缺口询问；未获授权的合并、部署、发布和删除不由修复或提 PR 请求自动授权。
- 技能导致询问、暂停或交付不完整时，给出实际读取的 `SKILL.md` 路径、相关原文和适用原因，区分明确要求与自己的解释。业务规则冲突无法判断时保留原条款，说明冲突和影响，只暂停依赖该决定的步骤。
- 只修改授权范围内的文件，保留用户已有改动；范围外问题列为建议。通用授权与验证规则由本文件维护，技能只补充领域条件，避免重复规定不同的确认流程。
- 完成意味着请求的交付物已实现、必要检查已通过、最终差异已核对，并说明改动、验证证据和剩余限制。必要检查失败或受阻时明确报告未完成项及原因，不把计划、局部通过或运行中的 CI 当作整体完成；用户只要求检查点时按该边界交付。

## 3. denial 桌面 QEMU 速查（macOS arm64；完整步骤与坑见 docs/sop-run-starryos-denial-qemu.md，改启动方式或板卡假设时同步更新该 SOP）

- 编译：先设 musl 工具链 PATH 与 `BINDGEN_EXTRA_CLANG_ARGS_aarch64_unknown_linux_musl`（lwprintf-rs 的 bindgen 需要，配方在 SOP §1），再 `cargo xtask starry build -c os/StarryOS/configs/board/qemu-aarch64.toml --smp 4`；跑完 unset 该环境变量。
- 启动：HVF + `-smp 4` + virtio-gpu/input + nvme rootfs + cocoa 显示（SOP §2），登录后 deniald 命令必须带 `--flutter-bundle /opt/denial/shell-bundle` 与 `LD_PRELOAD=/usr/lib/libdenialshim.so`（SOP §3）。
- 关键坑：漏 `--flutter-bundle` 会静默降级诊断模式（蓝闪→永远黑屏+光标，唯一线索是 dd.log 的 `presentation="diagnostic-atlas"`）；不能硬杀 QEMU（guest ext4 数据块丢失、文件变全零），关机用 guest 内 `sync; poweroff`；同一 rootfs 镜像不能双开（写锁）。
- 性能基线（旧调度器快照树）：LP_NUM_THREADS=1 锁屏约 t50s，默认多线程约 t90–150s（多线程 llvmpipe 反而慢约 3 倍）；根因是唤醒/派生永远本地放置、无负载均衡，上游 PR #1775（2026-09-08 调度器重建）已修复，repatch 后更新基线。
- 诊断工具（2026-09-09 实战验证）：QEMU 加 `-gdb unix:/tmp/prof/gdb.sock,server=on,wait=off`，串口用 `-chardev socket,...,server=on,wait=off,logfile=console.log` 并由常驻进程持有连接（chardev socket 断开后重连会被饿死，gdbstub 有独立监听不受影响）；采样循环 `vCont;t → ? 确认 T05 → 逐 vCPU g 包读 PC/CPSR（CPSR.M[3:2] 区分 EL0/EL1）→ vCont;c`，符号化用同目录 `starryos` ELF。脚本在会话 /tmp/prof/{probe,flame,guesthold}.py。
- 已证实结论：快照树 `/proc/stat` 的 sys 包含线程阻塞期间的 off-CPU 时间（`TimeManager.poll` 在下次切换补记全量 delta），top 的 99% sys 是记账幻象，以采样器为准；真实瓶颈是老调度器把全部用户任务放在 CPU0（采样 34/34 个用户态样本全在 tid1）、三核空转，内核侧非空闲热点为 `AddrSpace::can_access_range` 逐页探测与 epoll 扫描（上游 #2261/#2302 已针对性优化）。
- 根因链与实验结论（2026-09-09）：deniald/flutter 存在用户态时钟轮询（Linux 上走 vDSO 零成本），StarryOS 无 vDSO 使每次时钟读成为真系统调用并走 `prepare_user_memory` 慢路径拿 aspace 全局锁，自旋线程近乎垄断该锁导致同进程全部线程的系统调用饿死（采样可见 26% busy 全部集中在 tid1 的 `can_access_range`/procfs 路径）。启用 `starry-kernel/user-access-fastpath`（board 配置 feature）可使风暴消失、busy 分散多核，但在快照树上桌面黑屏不渲染（A/B 验证：回退后 +30s 锁屏出画），故回退保持 baseline；最终修复依赖上游 #2261/#2302 重写的 UserAccess/VMA 路径，随 repatch 落地。另：串口 chardev 在 guest 忙时输入饿死、>4KB 输出冻死，诊断用 gdbstub 采样 + HMP screedump 亮度代替。
- 诊断证据与现状补充（2026-09-09）：自旋证据 20s 窗口 44/172 busy 全在 tid1、x2=0x10（16 字节 timespec）、LR=prepare_user_memory+0x27c；fastpath A/B 用 HMP screendump 亮像素做指标（回退后 +30s 出画 16.5% 稳定 vs 开启永黑）。工具坑补遗：僵尸串口守护进程会偷 FIFO 命令行（重启实例先 pkill）；串口积压以分钟级消化，仅 boot 阶段可靠。当前 baseline QEMU（无 fastpath）保持运行、锁屏可见供观测；关机优先 guest `sync; poweroff`，串口失联时退 HMP system_powerdown+quit（rootfs 活跃写极少、有备份兜底）。repatch 范围：上游 #1775+#2313（调度器）、#2261+#2302（UserAccess/VMA 重写，根治锁饥饿）、移植 #2295/#2296/#2284。

- 上游吸收结果（2026-09-09）：repatch 完成于 merge c4a00deee（origin/dev 终态，26 个提交，含 #1775+#2313 调度器重建、#2261+#2302 UserAccess/VMA 重写）+ 接缝修复 fa91fc。结构剧变：axtask→components/ax-task（cpu/remote 远程调度器、PI 互斥体、safe-point 校验），axpoll 重写为类型化就绪能力、唤醒注册表移入新 crate axpoll-set，evdev 改为专用 evdev-irq-service 线程（register-before-park）——本地 #2296 兜底轮询随之作废未再移植，键鼠由上游 IRQ 路径负责（待 cocoa 实测）。本地保留：#2295（display/rtc board）、#2284 IN_FENCE_FD、dma-buf lseek、KMS 活跃状态回读（handle_get_plane 已改 Card0 方法以适配 #2313 UserTaskRef 签名）。
- 新树 panic 排障（2026-09-09）：deniald 启动即 `validate PI mutex blocking context failed`（上游 open bug #1709 同类）。诊断法：临时把 axruntime panic_shutdown 改 wfi 挂死 → gdbstub 事后抓栈；再在 axruntime validate_schedule_context 加临时 GUARD-DEBUG（guard 状态读数）+ 内核栈扫描（无 FP，扫 SP 起 2KB 内核态字，宿主 nm 符号化）。定锤：irq=0、baton=Finished、hard_irq=false、**arch_preempt_depth=1**；栈 = sys_connect → ax_net UnixSocket::start_connect（持 remote_addr SpinLock=禁抢占 guard）→ UnixNamespace::resolve → ext4 Inode::lookup_locked 争用 SleepMutex → 阻塞校验拒绝。根修 73293a874：bind/start_connect 在锁内仅占位地址，namespace 操作移出 SpinLock 临界区、失败回滚（上游 stream.rs 注释本就声明该锁层级意图）。红→绿：恐慌栈 → 修复后零恐慌 + 完整桌面。
- 新树基线（2026-09-09，待多次复核）：LP_NUM_THREADS=1 锁屏出画 <35s（旧树 t50s）；锁屏右上角 CPU 1%（旧树 99% sys 记账幻象消失）；空闲采样 248 样本 88.3% 为 WFI idle、零 can_access_range/prepare_user_memory 热点、零锁自旋——aspace 锁饥饿随 #2261/#2302 根治（AT 探测快路径已默认开启，无需 user-access-fastpath feature）。
- 工具坑补遗（2026-09-09）：QEMU 接 gdbstub 后 vCPU WFI 会推停机帧，断点监听须「停机→查 PC→非目标则 vCont;c 续跑」循环，且轮询停机流会把 guest 串口饿死——诊断用「panic 后挂死再附加」，不要全程附带 gdb。release 内核无帧指针，panic 内建 backtrace 输出 BT_ERROR unsupported，栈扫描+宿主符号化是可靠替代。ax-net clippy 在 macOS 宿主因 cpu-local 的 `.percpu.*` section 名不合法而失败，属环境限制（上游 CI 在 Linux ELF）。X11 目录现象：/tmp/.X11-unix 偶发在 mkdir 后、deniald 启动前消失（smithay 只删文件不删目录，机制未明），重跑 mkdir 再启动即可绕过；SOP §2 的 mkdir 步骤后应加存在性确认。
- 键鼠修复与端到端验证（2026-09-10，上游 issue #2352）：cocoa 鼠标事件丢失（点击无反应、光标不动）根因是 virtio input probe 走 `take_virtio_transport_masked`（上游 #1228 轮询时代遗留），probe 即置 PCI command 的 `INTERRUPT_DISABLE` 且驱动 `enable_irq()` 只改软件标志，设备物理上发不出 INTx；而 IRQ 绑定其实解析成功（GPEX legacy 线 → GIC SPI 3/6，domain 7），evdev-irq-service 线程注册正常，事件也确实进了 virtqueue——唯缺中断。修复：input probe 改回 `take_virtio_transport`（与 net 一致，drivers/ax-driver/src/virtio/input.rs 单行）。判别手法：先启动阻塞 `dd if=/dev/input/eventX` 再 HMP 注入（0 字节+Terminated=唤醒断；预注入后读有数据=read 路径本身通）。修复后阻塞读者秒唤醒（键盘/鼠标各 144B，dd Done）；桌面端 libinput 枚举 event0/event1、合成器光标任意定位、点击关面板、拖拽开 Dashboard 全通。大坑：probe 日志 `registered virtio input device irq=None` 打印的是 `irq_num()`（绑定的 legacy 原始编号视图，native/GIC domain 绑定恒为 None），与实际可用的解析结果相反，误导整条排查线（先按 irq=None 实现了轮询兜底，GUARD-DEBUG 探针显示 irq=Some 才定锤，兜底已回滚）。遗留：display 也用 masked transport（靠轮询路径工作正常，不动）；QEMU virt GPEX interrupt-map 只覆盖根槽位 0-3，槽位 4-7 设备要走 MSI 需先实现 GICv2m provider（rdif_msi，FDT 已有 msi-map/v2m frame，NVMe 日志"PCI MSI provider is not registered"即指此）。
- 帧率根因定锤（2026-09-10，M4/HVF 桌面"卡"主因）：桌面被 **0.5Hz 的 vsync 节拍门控**，与算力无关。三条独立证据：DENIA_RENDER_AUDIT 的 `vsync_gap` 恒 ≈2000283µs（±3ms，非测量噪声）、Volition 审计 `presentation_interval≈1.97–2.0s`、HMP 连续 screendump 差分实测拖拽动画 11s 仅 3 次画面变化（≈0.28fps，拖拽期间一帧不出）。card0 内核 TEMP-PROF ioctl 探针（已回滚）显示：MODE_ATOMIC 稳态每 2.000s±5ms 一次、全程**零** WAIT_VBLANK/CRTC_GET_SEQUENCE/QUEUE_SEQUENCE 调用，且启动期 `DRM_IOCTL_SYNCOBJ_EVENTFD(0xc01864cf)`、0xc0186449、0xc0106443 三项 ENOSYS——Volition 的 vblank 时钟 API 全不可用，退化到内部 ~2s 的 KMS-hold 兜底节拍当 vsync。内核 present 路径本身微秒级（audit：fence_to_submit 2.8ms、submit_to_presentation 0、delivery 236µs；card0 present=4MB memcpy+transfer_to_host_2d+resource_flush 同步往返，每帧约几 ms，非瓶颈）。次要发现：(a) guest 事件基元实测健康（timerfd+epoll 200ms 精准、eventfd/pipe/clock_nanosleep 正常，空闲与桌面带载均复测），排除内核 timer/epoll 回归；(b) `sched_setscheduler` 对 SCHED_RR/FIFO 返回 ENOSYS，deniald 的 latency-critical 提权全部失败（18:28 后 fallback 到 SCHED_OTHER nice=-10），是帧延迟抖动的次要因素；(c) llvmpipe 光栅化 LP_NUM_THREADS=1 稳定 50–80ms/帧（raster_avg_us，新调度器下多线程仍病态：6ms–1223ms 波动），即使修好节拍单线程也只有 12–20fps。修复方向：card0 补 vblank 时钟（CRTC_GET_SEQUENCE/QUEUE_SEQUENCE + flip 事件按 60Hz 边界盖时间戳或内核合成 vblank 事件流），llvmpipe 多线程与 sched_setscheduler 属第二梯队。工具坑补遗：QEMU 11 HVF 下 gdbstub 的 Z0/Z1 断点均不触发（vCont;t 停机可用）；StarryOS ptrace 为 per-process 附加语义（一进程同时只允许一个 tracer），tracer 挂死时 `kill -9` 触发的 detach_live_tracees_of 曾使 guest 串口失联需重启；gdbstub 采样工具仅能看"正在运行"的 PC，查"阻塞在哪个 syscall"需 ptrace strace（guest 有 musl gcc 可自编译）或内核探针。
- 帧率根因修正与 vblank 时钟落地（2026-09-10，接上条）：带载复测推翻「0.5Hz vsync 门控」主因结论——HMP 连续注入鼠标时 MODE_ATOMIC 间隔从 2.0s 缩到 **0.10–0.13s（≈10fps）**，证明 KMS 管线按需运转、flip 回收链健康（audit：`output_ticks=60`、`missed_vblanks=0`、`presentation_to_submit=2.0s` 而管线各段 <70ms）。2.000s 空闲节拍是 Dart 场景静止时的需求节拍（锁屏无动画），非内核门控；Volition 从未退化到「KMS-hold 当 vsync」，其提交节奏 = presentation_target−2ms + 1-in-flight 等 flip。真实带载瓶颈是 llvmpipe 单线程 raster（50–80ms/帧 → 10–13fps），与 audit `raster_avg_us` 一致。上条的 0xc0186449/0xc0106443 实为 **VIRTGPU_GET_CAPS/RESOURCE_CREATE**（mesa 探测 GPU 加速，非 vblank API）。已落地：card0 合成 60Hz vblank 时钟（`pseudofs/dev/vblank.rs`，序列号=自建卡起单调时间÷周期，对齐 Linux `vblank_disable_immediate` 模式）+ `CRTC_GET_SEQUENCE(0x3b)`/`CRTC_QUEUE_SEQUENCE(0x3c)` + `WAIT_VBLANK` 按 4.19 `drm_wait_vblank_ioctl` 重写（absolute-0 即查询、`_DRM_VBLANK_EVENT` 入队、SIGNAL/未知位 EINVAL、inactive CRTC EINVAL、missed 即发）；queued 事件由 `poll()` 惰性兑现（无内核定时线程，延迟≤调用方 poll 间隔）。回归：`drm-test-drm-modeset` 红（23 fail，errno=95）→绿（75/75）。SYNCOBJ_EVENTFD 明确不实现：smithay `supports_syncobj_eventfd` 探针期待 ENOENT，部分实现会让 deniald 宣告 linux-drm-syncobj-v1 而后继 FD_TO_HANDLE/TIMELINE_SIGNAL 等全族失败，保持 ENOSYS（隐式同步是当前工作路径）才是诚实语义。桌面 A/B：新内核锁屏出画正常、带载 screendump 差分 8/8 全不同（0.45s/帧），无回归；帧率改善仍待 llvmpipe 多线程（P1）与 virtio-gpu 3D 加速（VIRTGPU_* 族）。
- 上游采纳记录（2026-09-10 核实）：我们向上游 rcore-os/tgoskits 提交的两个 PR 已合并进 origin/dev——#2295（fix(starry): enable display and rtc on the qemu-aarch64 board，2026-09-09 合并，merge commit d9018b65a）与 #2279（fix(axbuild): validate rootfs extraction and drop broken fakeroot wrap on non-Linux hosts，2026-09-10 合并，merge commit 81d50f67a，上游附签 周睿）。注意当前分支（fix/card0-vblank-clock）尚不含这两个合并提交：#2295 的内容本地已原样保留（工作树与上游版 diff 为空，同步 origin/dev 时零冲突），#2279 即本地原补丁。仍在上游开放的 PR：#2353（input probe INTx unmask）、#2296（evdev poller）、#2284（card0 KMS）、#2280（axbuild 宿主工具链回退）。
- 本机测试工具链新知（2026-09-11）：上游 #2361（axbuild 测试执行迁移 shell check steps，9-10 进 dev）之后，grouped/c 流水线资产准备硬性要求宿主 qemu-user 二进制（`find_host_binary_candidates(qemu_user_binary_names)`），而 qemu linux-user 无法在 macOS 宿主构建——本机冷缓存永远无法准备 c 子用例（此前能跑通全靠缓存命中掩盖）。macOS 兜底 = 本地 #2280（e03830bf7 `resolve_guest_tool_execution`：有 qemu-user 走 Emulated，否则回退宿主原生 `{gnu_tool_prefix}-{tool}` 交叉工具，prebuild.sh 响亮跳过；open PR #2280）。规则：rebase/同步 origin/dev 后在本机验证 Starry 测试套件，必须同时把 e03830bf7 cherry-pick 进本地验证分支（不进上游 PR，上例 local/vblank-validation = 7df3bf6f5 + a22299ad8）。Ubuntu 过渡态构建虚拟机（~/arm64-buildvm/vm.qcow2，cloud-init 用户 build / 密钥 ~/.ssh/arm64build，`qemu-system-aarch64 -M virt -cpu host -accel hvf` + hostfwd 2222）约束在最小行为集，不用于常规本地验证；2026-09-11 试验痕迹已清理（删 ~/tgoskits 与 musl-gcc 符号链接）并 guest 内 poweroff。
- 上游 Issue/PR 进度（2026-09-11）：帧率排查结论落地为上游 Issue #2364（card0 无 vblank 时钟：WAIT_VBLANK 语义反转 + CRTC sequence ENOSYS + flip 时间戳失真）与 PR #2365（feat(starry-kernel): synthesize card0 vblank clock with CRTC sequence ioctls，单提交 rebase 到 dev tip 481d6bdc4，fork 分支 feat/card0-vblank-clock，"Closes #2364" 回链；本地回归 75/75 于 local/vblank-validation 实测）。
- RT 提权打通与 MT 重测（2026-09-11，快照栈 fix/card0-vblank-clock 实测）：9-10 记录的 sched_setscheduler ENOSYS 来自还在跑旧快照内核的 baseline 实例，结论作废——新树 RT 全链可用：deniald `armed non-fatal realtime CPU scheduling priority=1` + `elevated latency-critical thread="flutter-raster" policy=SCHED_RR value=1 source=direct`。llvmpipe 默认多线程不再病态：带载 raster_avg 6.4–13.9ms、p95/max 30–77ms（旧数据 6ms–1223ms 波动消失；旧 LP=1 的 50–80ms 反比 MT 慢 4–5 倍）。修复方向更新：llvmpipe MT 与 sched_setscheduler 两项从待修清单划掉；下一瓶颈是动画期 presentation 节拍（vsync_gap p50≈83ms≈12fps present，而引擎 6–14ms/帧可跑 60fps+），需查 Volition 提交节奏与 vblank 事件投递路径。
- 桌面自动化测试手法（2026-09-11 定型，工具 /tmp/prof/mouse.py）：HMP `mouse_move` 瞬移（大步长）不会触发 UX 动画，且相对位移在边缘被钳制、累积漂移导致测试不可复现——每次测试前必须先用 corner 步骤（固定小步长打到钳制原点）校准，再用小步长+定时间隔平滑滑移（≈8px/5–12ms），点击用 `mouse_button 1`/`0`（press/release 分开发送，间隔≥120ms）。HMP screendump 每次 ~3MB 且暂停 vCPU，会污染 DENIA_RENDER_AUDIT 统计窗口（vsync_gap 被拉到 300ms+），审计读数必须在无截屏窗口采集。
- rebase 栈内核 panic（2026-09-11，待归因）：origin/dev tip(481d6bdc4)+vblank+#2284+#2280 栈上 deniald 启动即内核 panic `ax-task/src/sync/mutex/mod.rs:548 validate PI mutex blocking context failed: operation requires a scheduler safe point`（#1709 同族），与 RT 是否提权无关（RLIMIT_RTPRIO=0 也复现），panic 后 ARCEOS_PANIC_EMERGENCY 停机；同 night 的快照栈无此 panic。嫌疑面为 c4a00deee→481d6bdc4 增量（含 ax-percpu cpu-local 0.3 重发布 #2355）或 cherry-pick 接缝。归因前 PR #2365 的桌面级验证以快照栈为准（QEMU 回归 75/75 已在 rebase 栈独立通过）。
- futex 唤醒批次 panic 定锤与修复（2026-09-11，快照栈）：桌面带载两次把 QEMU 打死，panic `starry-kernel/src/task/futex.rs:543 push_wake: one futex wait generation cannot enter two live wake batches`（第一次 kill 多线程 deniald、第二次纯鼠标动画，均在 ~5–12s 内）。根因：waker 在 domain 锁内选人（mark_woken+unlink）后、`wake_all()` 前被抢占，等待者由独立 deadline 唤醒→finish 该代次→用户态立刻重新入队新代次并再睡，第二个 waker 选中新代次 push 时撞上 `ThreadCore.wake_batch_linked=true`（前一批次未 drain）。该窗口对 pthread timedwait/cond 高频负载必然打开。修复：`push_wake` 弃 assert 改合并语义（push 返回 false 即丢弃，前一批次必然送达唤醒，futex 重查协议容忍虚唤醒）——与 ax-task wake_batch.rs 文档契约、Linux `wake_q` 合并行为、上游 PI 图代码三方一致，断言属离群实现错误。回归：axtest 20 号 `requeued_waiter_coalesces_with_undrained_batch`（真实 UserTaskRef 等待者 + 内核线程 waker + deadline 驱动重入队，RED=旧代码精确 panic、GREEN=修复后通过且 coalesced>0 证明竞态路径被行使；axtest_kernel 全套 176 过）；桌面 60s 高强度动画零 panic（5×历史 panic 窗口）+ 动画期 23/23 帧变化无回归。axtest 手法沉淀：axtest 组装可调度任务必须用 ROOT_PID_NS（新根 namespace 的 identity 无 root 绑定，Thread::new 即 panic）；`wait_if` 超时返回 Err(ETIMEDOUT) 是正常路径；`cargo xtask ktest qemu -p starry-kernel --features axtest,smp --arch aarch64 --test axtest_kernel` 为内核 axtest 入口。本机 clippy 全矩阵不可行（riscv64/x86/loongarch 的 musl 交叉 gcc 未装，lwprintf build.rs 裸找 `{arch}-linux-musl-gcc`），改动只涉及 aarch64 时手工匹配参数跑该目标即可（命令=AGENTS.md §2.1 口径 + `--target scripts/targets/bare/aarch64-*.json -Z json-target-spec -Z build-std=core,alloc` + BINDGEN 环境变量）。
- denial 锁屏与测试环境（2026-09-11）：自动锁屏策略在 Dart 设置（默认 idleLock 5min + DPMS 10min），持久化 `$HOME/.config/denial/settings.json`（schema version 21，compositor 启动加载；写坏 fallback 安全默认并 WARN，不覆盖文件）。本 rootfs 已写 power.idleLockEnabled/idleDpmsEnabled/idleSuspendEnabled=false 并经 Dart 写回合并、已确认加载；root 密码已改 1234（锁屏面板要 PAM 密码，锁屏点击/滑动唤出面板后 `sendkey` 注入）。本机已默认无锁启动（冷启动直接桌面），锁屏只在自动策略触发后出现。环境坑：guesthold.py（串口持有守护）在 QEMU 运行中 pkill 会留下僵尸 accepted 连接，此后新连接进不去、TX 冻结、guest shell 全冻——恢复只能 HMP system_powerdown+quit 整机重启；guest 内 `kill deniald`（多线程唤醒风暴）同样触发本条 futex panic（已修复）。/tmp 是 tmpfs，dd.log 不落盘，串口 TX 分钟级积压且 >4KB 冻结——审计数据要么趁串口健康即取、要么整 guest 重启后立刻拉取。
- presentation 节拍排查半程（2026-09-11）：deniald 帧调度是软件时间线（OutputTimeline 自推 next_tick + 相位锁，calloop 定时器驱动，不靠内核 vblank 事件 tick；相位锁每帧最多调 250µs，与 83ms 拍子无关）；提交经 Volition deadline 调度器贴近 presentation_target 发非阻塞原子提交（提交即同步呈现，card0 语义匹配）。vsync_gap p50≈83ms 是 Dart 源（引擎需求节拍）而非 KMS 门控：llvmpipe MT 带载 raster p50 17.8ms/p95 73ms 是主吃项；空闲 ~2Hz 按需呈现正常。卡点：串口冻结拿不到带载 output_scheduler 审计（presentation_interval/missed_vblanks 真值），HMP 截屏差分只能给 ≥6fps 下限。下一手：整 guest 重启后立刻跑「动画+即取 dd.log 审计」一体流程；presentation_interval p50 若 ≈83ms 则查 Volition 提交到 KMS 的排队路径，若 ≈70-100ms 且 deadline_to_presentation p99 大则查 RT 调度延迟。
- 全套 QEMU 回归两连击定案（2026-09-11，快照栈）：全套 473 用例首跑暴露两处真回归，均已定锄修复。(1) `drm-test-drm-modeset` 在 grouped `qemu/system` 必挂而单跑恒绿——根因是 card0 全局 KMS 状态跨进程泄漏：先行用例 test-drm-atomic 做过真实 commit 后退出，其 ModesetState/fbs 表遗留，后一个纯 legacy 用例 RMFB 后 `crtc_active()` 因 `state != default` 误判活跃，GET_SEQUENCE 应 EINVAL 却 ret=0。修复 = 落实 Linux `drm_release`→`drm_fb_release` 契约：Card0 记 per-fd open_count（仿 Tty），最后一个 fd 关闭时清 atomic state、legacy 绑定、fbs 表、事件队列（`reset_kms_state`）；`crtc_active` 判据改为「活 fb 绑定」（legacy fb 或 atomic plane_fb 且 fb 仍在表）。注意 DeviceOps::close 是 per-fd 调用（File::drop），判"最后一个"必须 open_count 减到 0（Tty 已有同款模式）。(2) 排查手法：xtask 后台任务 stdout 只留尾部，失败名单必须重跑 `-c qemu/system` 重定向全量日志 grep `STARRY_SYSTEM_TEST_FAILED`；单用例 `-c qemu/system/<case>` 与 grouped 的差异即"跨用例状态交互"的金标准诊断。终态：grouped system 1/1 全绿（含 drm-atomic→modeset 顺序）、drm modeset/atomic 单跑绿、futex axtest 176 过、桌面 60s 动画零 panic。两个修复提交：3c8935a47（futex 合并语义+回归测试）、fd2266548（card0 生命周期重置）。
- 上游采纳与抢跑记录（2026-09-13 核实）：#2280 已于 9-11 合并进 dev（187b306db，标题定为 reuse cross-tool wrappers for native binutils）——「rebase 后必须 cherry-pick e03830bf7」的本地兜底规则自此作废，macOS 本地验证直接用上游实现。dev tip 前进到 9383ba409（release #2384，481d6bdc4→tip 新增 28 提交，含 #2274 perf、#2327 rknpu、#2386 someboot、#2387 rdrive）。更重要：#2357（refactor(ax-task): unify thread lifecycle and PREEMPT_RT lock semantics，a99d7af5c）已独立修掉 futex push_wake 断言——与新 dev 上 push_wake 直接丢弃 push 返回值、注释点明「wake_q 节点可能仍属于更早的 selector」，且自带回归测试（coalesced_scheduler_wake_still_counts_each_selected_wait、coalesced_wake_observes_published_state、wake_q store/load 试金石）——我们 3c8935a47 的修复+测试被完全覆盖，futex issue/PR 放弃，冗余 fork 分支 fix/futex-wake-batch-coalesce 已删；本地修复保留在快照栈（等 rebase 时自然对齐上游）。
- card0 生命周期修复已上游化（2026-09-13）：Issue #2392（bug(starry-kernel): card0 KMS state leaks across clients，grouped 必挂单跑恒绿的跨进程状态泄漏）+ PR #2393（fix(starry-kernel): reset card0 KMS state when the last client closes，Closes #2392 回链）。分支 fix/card0-kms-release-reset 基于 dev tip 9383ba409 单提交 320e9b4e4；与本地 fd2266548 的差异：不含 crtc_active 判据改写（那是 #2365 vblank 时钟引入的符号，dev 上不存在，PR body 明确留给 #2365 一并改）。PR body 记录了「为什么挂 fd 生命周期而非 ioctl」「为什么连事件队列一起清」「为什么用计数而非 exclusive 标志」三条设计逻辑。mergeable=True，CI 在跑。#2296 标题被改为 refactor(ax-input): remove obsolete evdev polling fallback（语义从 fix 转向 refactor）。
- dev tip 桌面验证栈与帧率根因终锤（2026-09-13）：上游状态无新增提交（dev tip 仍 9383ba409），#2365（vblank 时钟）与 #2393（card0 重置）open 且 mergeable=True（unstable=CI 在跑），#2296 转 mergeable=True，#2284 仍 dirty。新验证分支 `local/dev-desktop-0913`（单提交 f5519af86）= dev tip + #2284 两提交 + fa91fca14（UserTaskRef 适配）+ 73293a874（ax-net unix 锁层级）+ 037396f78（input INTx）+ 7df3bf6f5（vblank）+ 320e9b4e4（card0 reset），卡点手工适配 dev 的 `vm_read(current)` API 与 `DRM_MODE_PROP_SIGNED_RANGE` 常量。**cherry-pick 陷阱**：#2284 分支基线在 #2268 之前，整提交 `cherry-pick -3` 会把 #2268 等无关基线差异全裹进来（上游标 dirty 的原因）——必须 `git diff c^ c -- <文件> | git apply -3` 按文件提取。桌面实测：RT 提权生效（`armed non-fatal realtime CPU scheduling priority=1`）、零 panic（9-11 rebase 栈 PI mutex panic 未在新 dev 复现，嫌疑解除）、vblank 时钟 `output_ticks=60/s` 稳定、grouped 前无回归。
- 帧率根因终锤（2026-09-13，同上栈动画实测）：83ms/12-15fps 缺口的全部延迟在**引擎帧生产段（deadline_to_ready），内核无责**。动画期 frame_scheduler 决策审计：dirty_output_ticks=60/s（引擎要 60fps）、unavailable_output_ticks≈46/s（ready 槽被上一帧占用）、实际 presentations=14-16/s。output_scheduler 延迟分解（15fps 窗口）：presentation_interval p50≈61-72ms ≈ deadline_to_ready p50 54-67ms + ready_to_submit 1-5ms + submit_to_presentation 0（提交即呈现）+ presentation_delivery 0.7-1.9ms。Dart 帧审计（frames=5 样本）：**raster_avg=91.6ms/p50=90.7ms/p95=127.6ms（llvmpipe 光栅化）占帧生产 95ms 的 97%**，build 仅 0.9ms、vsync_overhead 2.7ms、raster_queue 0.2ms；全部帧超 16.7ms 预算（engine_over_budget=5/5）。空闲期全链健康（2Hz 按需呈现、deadline_to_presentation 亚毫秒、submit_to_presentation=0、missed_vblank_streaks 与 sequence_delta 语义正常）。**9-11 的「MT raster 6.4-13.9ms」只适用于轻内容窗口，锁屏/桌面拖影内容 raster 87-127ms**——llvmpipe 光栅化时间强依赖内容复杂度，MT 收益不稳定。下一手全部在用户态：llvmpipe 光栅化（内容/分块/线程）或 virtio-gpu 3D 加速（VIRTGPU_* 族）；内核侧本轮无待修项。
- 桌面取证工具坑补遗（2026-09-13）：(1) guesthold 串口持有守护必须**单实例**——双 holder 分别抢走 ser.sock 与 serctl.sock 后出现「命令回显正常但响应永不出现」的假死；(2) QEMU chardev 被僵尸 accepted 连接污染后（pkill holder 的老坑）重连被饿死，只能整机重启换 socket 路径；(3) busybox grep -o 带正则类（`[0-9]*`）经常静默空输出，审计提取改用 `sed "s/\x1b\[[0-9;]*m//g"` 去 ANSI 后 `grep 关键字 | awk substr` 配对字段；(4) 动画后串口 TX 冻结是常态（分钟级），审计数据要在动画进行中或刚结束时用**单条小命令**即拉；(5) HMP quit 硬退后 rootfs 兜底：重启后 deniald 可能因 `/tmp/.X11-unix` 消失报 `Could not find a free socket for the XServer`，重跑 mkdir 循环确认存在后立即启动即可；本次实测 rootfs 数据完好（dd.log 审计、settings 均正常）。
- 上游采纳与精化（2026-09-15 核实）：#2353（input INTx）已合并进 dev（1f408d1cf，内容与本地 037396f78 在 input.rs 上逐字节一致），且 9-14 的 #2396（ff9d5f37e）立即对其精化——新增 `take_virtio_input_transport` 把 INTx 解掩时机从 probe 推迟到 IRQ action 安装之后（Linux「先装 action 再探测」生命周期；改 pci/mod.rs、drivers/ax-driver/src/virtio/input.rs、pseudofs/dev/event.rs），本地单行改回版本作废，分支 fix/virtio-input-pci-intx 可删。dev tip 9383ba409→b101144f9（+13 提交）：#2399 perf(sched) 内核 futex.rs 的 push_wake 保持合并语义并把 Waiter 预存 wake handle 改 `into_wake_handle()` 按需取（减少唤醒路径引用保留），另含 sleep EINTR/SA_RESTART 保留、单调睡眠免私有等待队列、axruntime guard 复用 IRQ 排除；#2389 axsched 新增容量感知 Fair placement（placement.rs）；#2270（Shi Lei）syscall/unix socket 语义对齐（unix/mod.rs 118 行：peer address、abstract bind 释放），**未触碰 connect 锁层级**；其余为 axvm（#2400/#2390）、LTP 迁移（#2404）、sysbench（#2395）、memcached/PMU/ltp-netstress 测试类。**73293a874（unix connect 锁层级）仍未被吸收**：origin/dev 的 `start_connect` 仍把 `with_slot`（VFS/namespace 工作）留在 `remote_addr: SpinLock` guard 内（bind 同构），PI mutex 阻塞校验 panic 隐患在上游潜伏，是唯一还值得上游化的本地接缝修复。PR 状态（9-15）：#2393 clean 且 CI 18 绿（下一个最可能被采纳）；#2365 unstable，CI 失败 2 项=loongarch64 Suites + std tests（**loongarch64 Suites 属上游维护、不关心**；std tests 待归因，4 项为级联取消），base 481d6bdc4 已落后需 rebase；#2296 unstable 但 30 检查全绿（疑似排队中）；#2284 仍 dirty。本地刷新（2026-09-15）：本地 dev 与 fork（Mchnan）dev 均快进到 b101144f9（旧集成态备份在 backup/dev-pre-upstream-pull-20260915；dev 独有 docs 提交 a7793b350 早已在 backup/dev-pre-upstream-pull-20260905）。下次桌面验证栈重导到 b101144f9：去掉 037396f78（上游已覆盖且更完善），保留 #2284 按文件提取 + fa91fca14 + 73293a874 + 7df3bf6f5（#2365）+ 320e9b4e4（#2393）。
- venus 加速 Phase 1 与路线修正（2026-09-15）：「宿主 MoltenVK + guest venus ICD」闭环启动。**架构级发现：vtest 跨 QEMU 边界不可行**——vtest 协议双向依赖 SCM_RIGHTS fd 传递（blob/shmem/fence，mesa 侧无条件断言 blob），fd 是内核局部资源，原定「guest vtest → 宿主 server」socket 方案在 QEMU 边界处死路，TCP relay 也救不了。Phase 1 改道 `-device virtio-gpu-gl,venus=true`（原 Phase 1 本就含 QEMU 构建）：渲染器与 MoltenVK 同在宿主进程侧，blob 映射走 QEMU memory API，guest 用真实 Linux（Alpine edge netboot）参与先验，StarryOS 内核零改动。Phase 0 PASS：molten-vk 1.4.2 枚举出 Apple M4，`VK_EXT_external_memory_metal`/`VK_EXT_metal_surface` 在位；MoltenVK 是 portability driver，应用必须开 `VK_KHR_portability_enumeration`（vkr 上游 vkr_instance.c 已自带该处理）。
- virglrenderer/QEMU darwin 构建定锤（2026-09-15）：virglrenderer tip cf6c62d 在 darwin 上 `-Dvenus=true` 可构建，坑是 `vkr_metal_helpers.m` include `venus-protocol/vulkan_metal.h`（上游装好的 venus-protocol 不带），用 shim 目录（链接 brew vulkan-headers 的 vulkan_metal.h）+ objc_args 注入 include 路径解决；venus-only 测试要 `virgl_test_server --venus --no-virgl`（无 no-virgl 时走 vrend EGL 初始化必挂）。宿主最小 vtest 客户端 smoke 通过（VENUS capset 40 dword 返回）。QEMU 11.0.3 darwin/venus 补丁共 4 文件：meson 的 virtio_gpu_gl/pci_gl 去 opengl 依赖（外层 `virgl.found() and opengl.found()` 门控也要改）、virtio-gpu-virgl.c 的 EGL 引用加 CONFIG_OPENGL 守卫 + v4 回调/ASYNC_FENCE_CB/THREAD_SYNC 移出 EGL 条件（vkr 硬性要求）+ `VIRGL_RENDERER_NO_VIRGL`（vrend 无 winsys 会中止 init）、virtio-gpu-gl.c 的 display_opengl 检查降级为 warn（无头）、ui/console.c 的 dpy_gl_* 断言改 NULL 容忍（无头丢 scanout）。构建坑：--disable-tests 不存在、contrib/plugins 的 C++ 示例要 -Dplugins=false、snappy/lzo2 链接要 LIBRARY_PATH=/opt/homebrew/lib。
- 16K 页 blob 映射坑（2026-09-15，本阶段最深的坑）：macOS arm64 宿主页长 16KB，guest Linux 内核按 4K 页粒度分配 hostmem blob 的 BAR offset，MAP_FIXED mmap 打在非 16K 对齐地址 → EINVAL → MAP_BLOB 失败（guest dmesg 表现为 response 0x1200 / command 0x208，ICD 挂死；QEMU trace + `-d guest_errors` 定位）。修复落在 virglrenderer：`virgl_renderer_resource_map_fixed` mmap 失败时由 -EINVAL 改回 **-EOPNOTSUPP**，QEMU virtio-gpu-virgl 既有 fallback（renderer 自选地址 mmap + memory region 子区域指向共享页）语义正确接住。ring blob(135K) 走 fixed OK，1MB blob 走 fallback OK。
- 验证环境沉淀（2026-09-15）：整套栈迁出 /tmp，落 **~/venus-stack**（src/qemu=v11.0.3 git 浅克隆+darwin-venus 分支、src/virglrenderer=GitLab clone+darwin-venus、prefix=安装前缀、mirror=Alpine edge aarch64 本地镜像+vmlinuz/initramfs-lts、qstart-alpine.sh 等脚本在根目录）。Alpine guest 采用 diskless netboot（HVF，6.18.42-lts）；slirp DNS 不可靠，用宿主 python http.server 起 APKINDEX+apk 本地镜像（内核参数 `alpine_repo=http://10.0.2.2:8000 modloop=...` 显式指定）+ 手写 APKINDEX 传递依赖解析器预下载；mesa-vulkan-virtio 26.2.2+vulkan-loader+vulkan-tools 装机 OK，内核确认 3 个 capset（VENUS id4）+ context_init。串口控制台丢字严重（回显/输出双向），对策=输入按 8ms/字符节流（alphold.py）+ 输出走 TCP 回传（recv.py 收 wget --post-file）。GitHub 侧：**Mchnan/qemu** fork 的 `darwin-venus` 分支（910b803）与 **Mchnan/virglrenderer-darwin**（master=上游 tip、darwin-venus=MAP_FIXED 修复 8107032）已推送；virglrenderer 上游在 freedesktop GitLab 故 GitHub 无 fork 源；最早误建的 Mchnan/virglrenderer 空仓库拒收推送且 token 无 delete_repo 权限，需手动删。
- venus Phase 1 现状与下一步（2026-09-15）：端到端链路已打通到 render server——vulkaninfo 建上下文（ctx 0x2 name vulkaninfo）、ring blob(135K) create/attach/submit OK，1MB blob 的 MAP_BLOB 曾因 16K 对齐失败、修复后 fallback 生效；**vulkaninfo --summary 最终读数与 offscreen 渲染回读尚未复测完成**（迁目录重建后待整栈重启验证，产物在 ~/venus-stack，git 版 QEMU 已构建完成）。SET_SCANOUT(0x103) 返回 0x1203 属 fbcon 无头 cosmetic，暂不处理。收尾动作：复测 vulkaninfo 枚举 Apple M4 → offscreen 渲染证明 → 结论回写 docs/research-host-vulkan-acceleration.md；Phase 2 立项=StarryOS card0 增 VIRTGPU_* 3D ioctl 族 + hostmem BAR 映射。现状画布 docs/venus-phase1-status.canvas.tsx（lint 3/3 PASS）。
- venus Phase 1 端到端复测闭环（2026-09-15）：**Phase 1 PASS**——`vulkaninfo --summary` 枚举出 `GPU0: Virtio-GPU Venus (Apple M4)`（vendorID 0x106b，DRIVER_ID_MESA_VENUS，mesa 26.2.2，apiVersion 1.4.343），offscreen compute 探针全链 VK_SUCCESS、4/4 读回匹配（0xc0de0000-3，M4 真实执行 guest dispatch）。完整证据链写进 docs/research-host-vulkan-acceleration.md §6。本轮三层新修复：(1) QEMU meson 外层门控——910b803 只删了内层 `opengl` 依赖，外层 `virgl.found() and opengl.found()` 未改，本机 epoxy 无 EGL 导致 virtio-gpu-gl 源集整个跳过（`-device help` 无 gl 设备的根因）；放宽为仅 virgl + `module_dep("ui-opengl")` 加 `#ifdef CONFIG_OPENGL`（module_load 对缺失依赖放弃加载），commit c9ce8a2；(2) HVF 16K 对齐崩 VM——1MB blob 4K offset 走 fallback 子区域后 `hv_vm_unmap` 对不对齐段 assert 杀 VM（HV_BAD_ARGUMENT hvf-all.c，此前误记为 hv_vm_map），改 warn+skip（commit 7109d5e），探针默认 TCG（qstart-alpine.sh 的 `QEMU_ACCEL` 可切回 HVF）；(3) `vkr_ring_start` 的 `thrd_create` 静默失败致 ring 停摆（guest 等死、render server 4 线程全眠、trace 冻结；sample/lldb 对该 worker 挂起难取证，改加 vkr_log 定锤），复跑未再现（疑 fork 后瞬态/旧库残留），日志已提交 darwin-venus。**新缺口（Phase 2 前置）**：ring-based fence retire 通知链未通——`vkWaitForFences` 5s 超时、vulkaninfo teardown 在 vkDestroyRingMESA 处理完之后挂死，ring 命令本身执行正常（读回已写好），需排查 vkr on_ring_seqno_update→sync queue→proxy retire_fence→QEMU→guest IRQ 链在 render-server 多进程模式下的接线。环境坑补遗：guest 必设 `XDG_RUNTIME_DIR=/tmp/xdg`；结构化输出只信 QEMU chardev logfile（alp_console.log 直写，socket 双读会竞争丢字），socket 通道仅注入命令；命令挂前台时后续输入只回显不执行，先发 `\x03` 解堵；交叉编译探针配方=aarch64-linux-musl-gcc（brew aarch64-unknown-linux-musl）+ vulkan-loader apk 解包取 libvulkan.so.1（补 libvulkan.so 符号链接再 -lvulkan）+ Alpine edge vulkan-headers apk + glslangValidator（brew）嵌 SPIR-V，产物放 mirror/ 由 guest wget。
- venus Phase 2 内核 3D 面端到端打通（2026-09-16，分支 fix/card0-vblank-clock 提交 28aa9a46c）：新 crate drivers/gpu/virtio-gpu（2D+3D 合并驱动：单控制队列 + pending FIFO，fenced SUBMIT_3D fire-and-forget——宿主把响应推迟到 fence retire，同步等待必卡死；命令头一律拷入驱动自有缓冲，曾踩 guest 栈 DMA 地址失效）+ ax-driver PCI SHARED_MEMORY_CFG 解析（cap64、BAR 物理重建，2D 不可用降级 3D-only：darwin venus-only 无 vrend，display 设备不注册否则 axdisplay 适配器 panic）+ starry-kernel vgpu.rs（card0 VIRTGPU_* ioctl 族：GETPARAM/GET_CAPS/CONTEXT_INIT/RESOURCE_CREATE_BLOB/RESOURCE_INFO/MAP/EXECBUFFER/GEM_CLOSE + 通用 syncobj 族全 timeline watermark；blob mmap key 从 1<<40 起、BAR slot 16K 对齐、PhysicalCached；状态挂 card0 reset_kms_state）。验证：raw ioctl 探针（tmp/vgpu-probe/vgprobe.c，debugfs 注 rootfs APFS clone + 串口 holder）21 步全绿含 guest 用户态 mmap 直写 hostmem BAR 回读一致；grouped system 1/1 回归。三个排障坑：(1) QEMU VIRGL_VERSION_MAJOR 宏被 virglrenderer 1.3 改名为 VIRGL_MAJOR_VERSION，未定义即 0 静默裁掉全部 blob/MAP_BLOB case（Phase 1 QEMU 是 brew 头在时构建的，patch 触发重编才暴露；修=源文件桥接宏名，已落 darwin fork）；(2) wire 命令号手抄错：CREATE_BLOB=0x10c、SUBMIT_3D=0x207、MAP/UNMAP_BLOB=0x208/0x209；(3) vkr shm blob 要求 blob_flags==精确 MAPPABLE、CTX_CREATE 空 debug_name 被 renderer 拒（补 starry-vgpu-{pid} 默认名）。遗留：fence 异步唤醒、GUEST blob、SET_SCANOUT_BLOB 接 KMS present（Phase 3）、deniald venus ICD 集成。

- fence retire 缺口修复（2026-09-15，Phase 2 前置清零）：根因两层叠加。(1) **macOS 无 eventfd**——virgl_util 的 `create_eventfd` 在 `HAVE_EVENTFD_H` 未定义时恒返回 -1，且 `virgl_renderer_init` 因 `has_eventfd()==false` **静默剥离 QEMU 传来的 THREAD_SYNC**（源码注释自称 "a hint"）；于是 client proxy 只剩 ASYNC_FENCE_CB，这是上游死配置：`proxy_context_init_fencing` 见无 THREAD_SYNC 直接 return（sync 线程不建），同步 retire 路径又被 `assert(!ASYNC_FENCE_CB)` + `virgl_context_foreach_retire_fences` 的 capset 守卫排除，**fence 在 client 侧永远无人退休**——render server 进程内 vkr sync 线程退休正常，但 proxy 的 shmem timeline 无人消费。(2) QEMU 二进制陈旧误导过一轮：源码 910b803 已含 `flags |= THREAD_SYNC`，但 03:15 的二进制没编进去（重编后仍 0x3c0 才暴露 init 剥离才是真因）。定锤手法：QEMU `-d trace:virtio_gpu_fence_ctrl,trace:virtio_gpu_fence_resp`（fence_ctrl=f guest 内核 CONTEXT_CREATE_FENCE 到达、fence_resp=BH 完成响应）+ proxy/vkr 插桩——fence_ctrl 有、fence_resp 无、server 侧 submit_fence/retire 全正常，断点精确落在 shmem timeline 无人消费。修复（virglrenderer b056c0d1，proxy_context.c）：ASYNC_FENCE_CB 置位即建 sync 线程，eventfd 存在则等 eventfd（Linux 不变）、否则 2ms 周期轮询 fence shmem；destroy 不再以 eventfd 为线程 stop/join 前提。回归：`vkWaitForFences: VK_SUCCESS`、fence_ctrl/fence_resp 完整往返、vulkaninfo --summary 干净退出（teardown 挂死消失）、读回 4/4 不变。协议事实（本轮读代码定锤，供 Phase 2 参考）：guest 的 fence 等待走 guest DRM syncobj timeline（`virtgpu_wait`），由 guest 内核在收到宿主 fenced response 后信号；ring 的 seqno 轮询（vn_ring_wait_seqno）只覆盖带 reply 的命令，fence 语义完全依赖 proxy retire 链。Mchnan/virglrenderer-darwin darwin-venus 已推 b056c0d1；qemu fork 无需新提交（源码本就正确）。
- SET_SCANOUT_BLOB 接 KMS present 闭环（2026-09-16，Phase 3 端到端 PASS，分支 fix/card0-vblank-clock）：card0 KMS present 按 fb backing 分流——`ADDFB2` 的 GEM handle 先查 dumb 表再查 vgpu blob 表，`present_fb` 的 blob 分支发 `SET_SCANOUT_BLOB`（宿主 surface 直接引用 blob 内存，零 memcpy），dumb 分支保持 memcpy 且 blob scanout 曾活跃时先重绑 2D 资源；`reset_kms_state` 资源拆除前先 disable scanout。驱动侧 `VirtioGpu3D` 增 `set_scanout_blob`/`bind_2d_scanout`/`disable_scanout`。E2E：探针 15 组（含关柄/尺寸负例）`=== VGPROBE PASS ===`，宿主 HMP screendump 像素级校验 guest 写入 blob 的渐变与品红（`=== SCANOUT-CHECK PASS ===`，零拷贝字节级证明）。四个新坑必须记住：(1) **HVF EPT 陈旧**——virgl MAP_FIXED 在 hostmem RAM 区块底下换页，hvf 监听器无感知 → guest BAR 写进孤儿页（KVM 有 mmu_notifier 兜底，Phase 1 能通是因为 TCG）；修复 = darwin fork 恒走 MR 子区域路径（内存监听器可见）。(2) **proxy 的 info.fd 是 shm fd 不是 dmabuf**——`res->base.dmabuf_fd >= 0` 使「fd<0 才回退」永不触发，落进 darwin stub `update_dmabuf` 假成功 → console 恒为 "Display output is not active." 占位；修复 = 非 GL console 无条件走 pixman 直显。(3) **blob 尺寸须 16K 对齐**——4K 对齐尺寸（如 0x21000）让 hvf subsection 被 skip-guard 跳过 → EPT 缺失 → guest 首次 BAR 访问卡死；驱动把线上尺寸统一取整到 16K。(4) **DRM↔virtio 格式映射**：XRGB8888 对应 `B8G8R8X8_UNORM(2)`（内存序 [b,g,r,x]，Linux 同表），X8R8G8B8(4) 字节序相反——映射错了扫描出颜色通道错位。另修 Phase 2 遗留：blob mmap key 改 4K 步进（sys_mmap 要求文件偏移页对齐，第二个 blob 起必 EINVAL）。验证工具三坑：debugfs 注入必须 kill_file+unlink+write+dump 校验（rm+write 产出截断镜像）、guest 运行中禁改镜像（unmount 写回覆盖）、holder ctl 线程与 reader 线程抢串口导致探针前 7s 输出丢失——校验器盯 chardev logfile 且 marker 匹配带偏移量基线（历史 marker 秒匹配=假证据）。工具：tmp/vgpu-probe/{vgprobe.c,scanout-check.py,run-starry-scanout.sh}；QEMU fork darwin-venus 新增 scanout 补丁（pixman 回退 + MAP_FIXED 禁用）。遗留 Phase 4：deniald venus ICD 集成（rootfs 装 mesa-vulkan-virtio，Impeller-Vulkan 走 VIRTGPU_* + 本 Phase scanout 上屏）。
